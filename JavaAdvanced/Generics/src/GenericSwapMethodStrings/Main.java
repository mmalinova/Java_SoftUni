package GenericSwapMethodIntegers;

import java.util.Scanner;

/*public class Main {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);

        GenericList<String> genericList = new GenericList<>();

        int n = Integer.parseInt(scan.nextLine());
        for (int i = 0; i < n; i++) {
            String input = scan.nextLine();
            genericList.getData().add(input);
        }
        String[] indexes = scan.nextLine().split("\\s+");
        int firstIndex = Integer.parseInt(indexes[0]);
        int secondIndex = Integer.parseInt(indexes[1]);

        genericList.swapElements(firstIndex, secondIndex);

        System.out.println(genericList.toString());
    }
}*/
